'use client';

import CameraController, { type CameraControllerProps } from './CameraController';

export default function CameraRig(props: CameraControllerProps) {
  return <CameraController {...props} />;
}
